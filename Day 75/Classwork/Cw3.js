function traverseArr(arr) {
for (let i = 0; i < arr.length; i++) {
    console.log(arr[i]);
}
}

const array1 = ["ვაშლი", "ბანანი", "ატამი"];

const array2 = [10, 20, 30, 40, 50];

traverseArr(array1);
traverseArr(array2);
